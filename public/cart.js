const remove_btn=document.getElementById('remove')

remove_btn.addEventListener('click',()=>{

const productId = button.getAttribute('data-product-id');
const username = document.body.dataset.username;  // safer



});
